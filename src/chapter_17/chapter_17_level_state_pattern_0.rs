// Recreate the state pattern blog post workflow from the book.
// Uncomment the main function and create the struct and methods so that the main function can compile



// fn main() {
//     let mut post =  Post::new();

//     post.add_text("I love Rust");
//     asset_eq!("", post.content());

//     post.request_review();
//     asset_eq!("", post.content());

//     post.approve();
//     asset_eq!("I love Rust", post.content());
// }

