// before completing the below exercises have a look at the documentation: https://doc.rust-lang.org/std/result/enum.Result.html

// exercise on DNA

// create a new type and use it as a collection for vector


// Write down the largest function in a generic way. see exo pg 185.
// this function takes as an input an array and iterates over it to find the largest item.
    // 

    /////////////////////////////////////////////////////////////////////////////////////////////////
    //////                                      Traits                                         //////
    /////////////////////////////////////////////////////////////////////////////////////////////////


    // lifetime with generics
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_vec_pop() {}
}
