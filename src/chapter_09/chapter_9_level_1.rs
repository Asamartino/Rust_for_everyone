// look at the different method before or while during the below exercises: https://doc.rust-lang.org/std/result/enum.Result.html

// complete the below function that returns the result of the division of num by den. It should return an error if den  equal zero
fn safe_division(num: u32, den: u32) -> Result< u32, &'static str>{
    todo!()
}


#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_vec_pop() {}
}
