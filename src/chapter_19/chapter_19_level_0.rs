fn main() {
    // Create two raw pointers one immutable and one mutable. They should both point to the same value
    // Note that this is only possible with raw pointers
    // Also note that, you only need the unsafe keyword if you tried to dereference a raw pointer
}
